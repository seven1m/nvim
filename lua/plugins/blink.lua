return {
  "saghen/blink.cmp",
  opts = {
    keymap = {
      -- LazyVim sets this to the "enter" preset, and I don't like that.
      -- I use Ctrl-Y to select the completion. :-)
      preset = "default",
    },
  },
}
